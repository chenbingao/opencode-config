# opencode 配置

个人 opencode 全局配置，同步到 `~/.config/opencode/`。

## 内容

- `opencode.jsonc` — 全局配置（LSP、compaction 等）
- `commands/` — 自定义命令（15 个 skill 快捷调用 + skill-manager）

## 使用

```bash
git clone git@github.com:chenbingao/opencode-config.git ~/.config/opencode
```
